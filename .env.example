# Backend API base URL — point this at your Spring Boot REST API.
VITE_API_BASE_URL=http://localhost:8082/api

# Supabase (optional — only if using Supabase auth/storage instead of Spring Boot).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
